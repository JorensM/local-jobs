// Core
import { StyleSheet } from 'react-native';

const text = StyleSheet.create({
    warn: {
        color: 'red'
    },
    title: {
        fontSize: 16,
        fontWeight: 'bold'
    },
    small: {
        fontSize: 14
    }
})

export default text;