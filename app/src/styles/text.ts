// Core
import { StyleSheet } from 'react-native';

const text = StyleSheet.create({
    warn: {
        color: 'red'
    },
    title: {
        fontSize: 16,
        fontWeight: 'bold'
    },
})

export default text;