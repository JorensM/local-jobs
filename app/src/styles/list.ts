import { StyleSheet } from 'react-native'

const list = StyleSheet.create({
    item: {
        padding: 4,
        width: '100%',
        borderWidth: 1,
        borderRadius: 4,
        borderColor: 'gray'
    }
})

export default list;