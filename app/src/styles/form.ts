// Core
import { StyleSheet } from 'react-native'

const form = StyleSheet.create({
    container: {
        gap: 4
    },
    input: {
        padding: 4,
        borderWidth: 1,
        borderColor: 'lightgray',
        height: 32
    }
})

export default form;